import {defineMessages} from 'react-intl'

const messages = defineMessages({
    'pos.listErrors.scopes.global': {
        id: 'pos.listErrors.scopes.global', 
        defaultMessage: 'Global errors'
    },
    'pos.listErrors.scopes.functionnal': {
        id: 'pos.listErrors.scopes.functionnal',
        defaultMessage: 'Functional errors'
    },

});

export default messages;
